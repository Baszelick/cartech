# Legacy Prisma Migrations

These migrations describe the superseded numeric-ID database model.

They are retained only for historical analysis and must not be applied to the
current schema. The active Prisma migration history is located in
`../migrations`.
